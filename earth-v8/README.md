# SirenFinder v8

SirenFinder v8 detects YouTube links in source KML descriptions and embeds those videos directly inside the corresponding siren popup. Embeds use YouTube's privacy-enhanced domain and are created lazily only after a user opens a siren, so they do not delay initial map rendering.
